{
  "name": "pristav-radosti",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "prelive:verify": "bash ./scripts/pre-live-verify.sh",
    "smoke:staging": "bash ./scripts/smoke-check.sh",
    "smoke:verify": "node ./scripts/verify-deploy.mjs",
    "monitor:health": "bash ./scripts/monitor-health.sh",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "pnpm": {
    "overrides": {
      "yaml": "^2.8.3",
      "brace-expansion": "^5.0.5",
      "flatted": "3.4.2"
    }
  }
}
