# Staging override — HTTP-only (no SSL)
# Usage: docker compose -f docker-compose.yml -f docker-compose.staging.yml up -d

services:
  nginx:
    volumes:
      - ./nginx/nginx-staging.conf:/etc/nginx/nginx.conf:ro

  api:
    environment:
      COOKIE_SECURE: "false"
      # Vyšší limit pro staging + E2E (prod default 10 / 5 min v apps/api/src/routes/auth.ts)
      AUTH_LOGIN_RATE_LIMIT_MAX: "120"
      AUTH_LOGIN_RATE_LIMIT_WINDOW: "5 minutes"
      # Force SQLite mode in staging — PostgreSQL schema is not set up for this deployment
      # Unset DATABASE_URL so the API falls back to file-based SQLite (DATABASE_PATH)
      DATABASE_URL: ""
