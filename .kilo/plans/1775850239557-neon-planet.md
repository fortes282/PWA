# Komplexní testovací plán — Přístav Radosti

## Základní problém stávajících testů

Existující E2E testy jsou **smoke testy** — načtou stránku a ověří nadpis. Nikdy nekliknou na
tlačítko a neověří, jestli to udělalo to, co mělo. Příklad:

```ts
// Stávající "booking" test — NESMYSLNÝ
test("4 — Booking flow", async ({ page }) => {
  await navigateTo(page, "/client/booking");
  const serviceCard = page.locator("button").filter({ hasText: /fyzioterapie/i });
  await serviceCard.click();
  // → ověří jen že se zobrazí krok s datem. KONEC.
});

// Co test MUSÍ dělat:
test("Klient dokončí rezervaci — termín se vytvoří, kredity se odečtou", async ({ page }) => {
  const balanceBefore = await apiGet(page, "/credits/balance");  // 4300 Kč
  // ... projde celý stepper ...
  await page.getByRole("button", { name: /Rezervovat|Potvrdit/i }).click();
  await expect(page.getByText(/Termín byl vytvořen|Rezervace potvrzena/i)).toBeVisible();
  // Termín se musí objevit v moje rezervace
  await page.goto("/client/appointments");
  await expect(page.locator("text=Fyzioterapie")).toBeVisible();
  // Balance se musí snížit
  const balanceAfter = await apiGet(page, "/credits/balance");
  expect(balanceAfter.data.balance).toBeLessThan(balanceBefore.data.balance);
});
```

---

## Seed data pro E2E testy

```
admin@pristav.cz      / Admin123!       → ADMIN
recepce@pristav.cz    / Recepce123!     → RECEPTION
terapeut@pristav.cz   / Terapeut123!    → EMPLOYEE (id=3, pracovní doby Po–Pá 8–18)
terapeut2@pristav.cz  / Terapeut123!    → EMPLOYEE (id=4)
klient@pristav.cz     / Klient123!      → CLIENT  (Martin Svoboda, balance 4300 Kč)
klient2@pristav.cz    / Klient123!      → CLIENT  (Eva Procházková, balance 6400 Kč)
```

Seed obsahuje: 7 služeb, termíny dnes/zítra/pozítří (CONFIRMED + PENDING),
záznamy kreditů, notifikace (nepřečtené), waitlist, zdravotní záznamy.

---

## ČÁST 1 — Nové E2E behaviorální testy

Každý test popisuje: **precondition → akce → assertion**. Všechny testy
po svém běhu cleanup dat přes API (aby neovlivnily ostatní testy).

---

### `e2e/interactions-client.spec.ts`

Nahradí část stávajícího `client-flow.spec.ts`. Testuje reálné interakce CLIENT portálu.

---

#### TEST 1.1 — Kompletní booking flow (vytvoření termínu)
**Precondition:** Jako terapeut vytvořit open slot přes `apiPost(page, "/booking-v2/slots", {...})`
na zítřejší datum s dostupným časem, nebo použít standardní `/appointments/available`.

**Akce:**
1. Přihlásit klienta (`klient@pristav.cz`)
2. Navigovat na `/client/booking`
3. Kliknout na kartu "Fyzioterapie" (1000 Kč)
4. V kalendáři navigovat na nejbližší dostupný den (kliknout šipku dopředu pokud dnes není slot)
5. Kliknout na dostupný časový slot (viditelný prvek s časem)
6. Kliknout "Rezervovat" / "Potvrdit rezervaci"

**Assertions:**
- Toast/banner `text=/Termín byl vytvořen|Rezervace potvrzena|Úspěšně zarezervováno/i` je viditelný
- `apiGet(page, "/appointments/upcoming")` vrátí array delší než před testem
- `apiGet(page, "/credits/balance").data.balance` je nižší než 4300

---

#### TEST 1.2 — Booking s nedostatečným kreditem
**Precondition:** Nastavit balance klienta na 0 přes API (nebo použít klienta s nulovým stavem)

**Akce:**
1. Přihlásit klienta
2. Pokusit se rezervovat službu za 1200 Kč

**Assertions:**
- Stránka zobrazí error `text=/nedostatek kreditů|nedostatečný zůstatek|insufficient/i`
- NEBO rezervační tlačítko je disabled s příslušnou hláškou
- `apiGet(page, "/appointments/upcoming")` — počet se nezměnil

---

#### TEST 1.3 — Zrušení termínu klientem
**Precondition:** Existuje CONFIRMED termín v seedu pro `klient@pristav.cz` (zítřejší)

**Akce:**
1. Přihlásit klienta
2. Navigovat na `/client/appointments`
3. Najít budoucí CONFIRMED termín
4. Kliknout "Zrušit" tlačítko u termínu
5. Potvrdit dialog / kliknout "Potvrdit zrušení"

**Assertions:**
- Termín zmizí z aktivního seznamu NEBO zobrazí se jako "Zrušeno"
- `apiGet(page, "/appointments/upcoming").data` neobsahuje ten termín
- Toast/zpráva `text=/Termín byl zrušen|Zrušení proběhlo/i`

---

#### TEST 1.4 — Odeslání žádosti o kredit
**Akce:**
1. Přihlásit klienta
2. Navigovat na `/client/credits`
3. Kliknout "Požádat o kredit" / "Nová žádost" tlačítko
4. Vyplnit částku: 2000
5. Vyplnit poznámku: "E2E test žádosti"
6. Odeslat formulář

**Assertions:**
- Toast `text=/Žádost.*odeslána|Žádost byla.*vytvořena/i`
- `apiGet(page, "/credit-requests")` vrátí žádost se statusem "PENDING" a amount=2000
- V recepčním portálu (přihlásit se jako recepce): nová žádost se zobrazí v seznamu

---

#### TEST 1.5 — Označení notifikace jako přečtené
**Precondition:** Klient má nepřečtené notifikace (seed: 2 notifikace pro klient id=5)

**Akce:**
1. Přihlásit klienta
2. `apiGet(page, "/notifications/unread-count")` → uložit počet (expect > 0)
3. Navigovat na `/notifications`
4. Kliknout "Označit vše jako přečtené" NEBO kliknout na první notifikaci

**Assertions:**
- `apiGet(page, "/notifications/unread-count").data.count` = 0 NEBO se snížil
- Badge na ikoně notifikací (NotificationBell) se aktualizoval

---

#### TEST 1.6 — Formulářová validace při bookingu
**Akce:**
1. Přihlásit klienta
2. Navigovat na `/client/booking`
3. Pokusit se přeskočit krok (kliknout "Dále" bez výběru služby)

**Assertions:**
- Nemůže přejít na další krok — stránka zůstane na kroku 1
- NEBO zobrazí se validation error

---

### `e2e/interactions-reception.spec.ts`

Testuje reálné interakce RECEPTION portálu.

---

#### TEST 2.1 — Schválení žádosti o kredit
**Precondition:** Existuje PENDING credit request (vytvořit přes `apiPost` jako klient, nebo
ze seedu pokud seed obsahuje pending žádosti)

**Akce:**
1. Přihlásit recepci (`recepce@pristav.cz`)
2. Navigovat na `/reception/credit-requests`
3. Ověřit že filtr je na "Čeká" a žádost je viditelná
4. Zadat reviewNote do inputu: "Schváleno pro E2E test"
5. Kliknout "Schválit" u první pending žádosti

**Assertions:**
- Žádost zmizí z PENDING filtru (nebo se zobrazí jako Schváleno při přepnutí filtru)
- Toast `text=/Schváleno|Úspěšně schváleno/i`
- `apiGet(page, "/credit-requests")` žádost má status "APPROVED"
- Kredity klienta se navýšily: `apiGet(page, "/credits/balance")` pro daného klienta

---

#### TEST 2.2 — Zamítnutí žádosti o kredit
**Precondition:** Existuje druhá PENDING credit request

**Akce:**
1. Přihlásit recepci
2. Navigovat na `/reception/credit-requests`
3. Kliknout "Zamítnout" u žádosti

**Assertions:**
- Žádost zmizí z PENDING filtru
- `apiGet(page, "/credit-requests")` žádost má status "REJECTED"
- Kredity klienta se NEzměnily

---

#### TEST 2.3 — Filtrování žádostí dle statusu
**Akce:**
1. Přihlásit recepci
2. Navigovat na `/reception/credit-requests`
3. Změnit select filtr na "Schváleno"

**Assertions:**
- Viditelné jsou jen žádosti se statusem APPROVED (nebo prázdný stav)
- Žádná PENDING žádost není viditelná

4. Změnit filtr na "Vše"

**Assertions:**
- Viditelné jsou žádosti všech statusů

---

#### TEST 2.4 — Označení faktury jako zaplacené
**Precondition:** Existuje faktura se statusem SENT nebo DRAFT

**Akce:**
1. Přihlásit recepci
2. Navigovat na `/reception/billing` nebo `/admin/invoices`
3. Najít fakturu která není PAID
4. Kliknout "Označit jako zaplaceno" / "Zaplaceno"
5. Potvrdit pokud vyskočí dialog

**Assertions:**
- Faktura změní status na "Zaplaceno"/"PAID" v UI
- `apiGet(page, "/invoices/:id")` vrátí `status: "PAID"`

---

#### TEST 2.5 — Vytvoření manuálního termínu (Reception)
**Akce:**
1. Přihlásit recepci
2. Navigovat na `/reception/calendar` nebo využít formulář
3. Kliknout na "Přidat termín" nebo time slot v kalendáři
4. Vybrat klienta: "Martin Svoboda"
5. Vybrat terapeuta a službu
6. Nastavit datum a čas (budoucí)
7. Uložit

**Assertions:**
- Toast o úspěšném vytvoření
- Termín se zobrazí v kalendáři / seznam termínů

---

### `e2e/interactions-admin.spec.ts`

Testuje reálné interakce ADMIN portálu — CRUD operace přes UI.

---

#### TEST 3.1 — Vytvoření nové služby (celý CRUD flow)
**Akce:**
1. Přihlásit admina (`admin@pristav.cz`)
2. Navigovat na `/admin/services`
3. Kliknout tlačítko "Přidat" / "+ Nová služba"
4. Vyplnit formulář:
   - Název: "E2E Test Terapie"
   - Délka: 45
   - Cena: 750
5. Kliknout Uložit

**Assertions:**
- Toast `text=/Služba byla vytvořena/i`
- Formulář se zavřel
- `page.locator("text=E2E Test Terapie")` je viditelný v seznamu
- `apiGet(page, "/services?includeInactive=true")` obsahuje novou službu

---

#### TEST 3.2 — Editace existující služby
**Precondition:** Existuje "E2E Test Terapie" z předchozího testu NEBO libovolná existující

**Akce:**
1. Kliknout edit ikonu (Pencil) u dané služby
2. Změnit cenu na 999
3. Uložit

**Assertions:**
- Toast `text=/Služba byla uložena/i`
- Cena "999 Kč" nebo "999,00 Kč" se zobrazí v řádku dané služby
- `apiGet(page, "/services")` vrátí novou cenu

---

#### TEST 3.3 — Validace formuláře nové služby
**Akce:**
1. Kliknout "Přidat" službu
2. Ponechat název prázdný, nastavit délku 2 (< min 5 minut)
3. Kliknout Uložit

**Assertions:**
- Toast `text=/Zkontrolujte|error|chyba/i`
- Formulář se NEZAVŘEL
- `apiGet(page, "/services")` nepřibylo nic nového

---

#### TEST 3.4 — Deaktivace služby
**Akce:**
1. Najít "E2E Test Terapie" v seznamu
2. Kliknout Trash2 / Deaktivovat ikonu
3. Potvrdit dialog `confirm()`

**Assertions:**
- Toast `text=/Služba je deaktivovaná/i`
- Služba zmizí z aktivního výpisu NEBO je zobrazena jako neaktivní
- `apiGet(page, "/services")` (bez includeInactive) neobsahuje "E2E Test Terapie"

---

#### TEST 3.5 — Vytvoření nového uživatele
**Akce:**
1. Navigovat na `/admin/users`
2. Kliknout UserPlus / "Přidat uživatele"
3. Vyplnit formulář:
   - Jméno: "E2E Testovací Klient"
   - Email: "e2e-client@test.cz"
   - Heslo: "TestPass123!"
   - Role: CLIENT
4. Odeslat

**Assertions:**
- "E2E Testovací Klient" se zobrazí v tabulce uživatelů
- Badge "Klient" u uživatele
- `apiGet(page, "/users")` obsahuje nového uživatele

---

#### TEST 3.6 — Vyhledávání uživatelů
**Akce:**
1. Navigovat na `/admin/users`
2. Zadat "Martin" do search inputu

**Assertions:**
- V tabulce je viditelný "Martin Svoboda"
- Ostatní uživatelé (Eva, Tomáš, ...) jsou skryti

3. Smazat hledaný text

**Assertions:**
- Všichni uživatelé jsou opět viditelní (8+)

---

#### TEST 3.7 — Filtrování uživatelů dle role
**Akce:**
1. Změnit role filtr na "Terapeut"

**Assertions:**
- Viditelní jsou jen EMPLOYEE uživatelé (Petr Dvořák, Lucie Horáková)
- Žádný CLIENT nebo ADMIN není viditelný

2. Změnit na "Klient"

**Assertions:**
- Viditelní jsou jen CLIENT uživatelé (4 klienti ze seedu)

---

#### TEST 3.8 — Deaktivace uživatele s ConfirmDialog
**Precondition:** "E2E Testovací Klient" vytvořený v TEST 3.5

**Akce:**
1. Najít "E2E Testovací Klient" v seznamu
2. Kliknout toggle/deaktivace tlačítko
3. V ConfirmDialog kliknout potvrdit

**Assertions:**
- Uživatel se zobrazí jako neaktivní (badge nebo šedé pozadí)
- `apiGet(page, "/users")` user má `isActive: false`

---

#### TEST 3.9 — Reaktivace deaktivovaného uživatele
**Precondition:** Deaktivovaný uživatel z TEST 3.8

**Akce:**
1. Přepnout filtr na "Neaktivní"
2. Kliknout "Reaktivovat" u "E2E Testovací Klient"

**Assertions:**
- Uživatel se zobrazí jako aktivní
- `isActive: true` v API

---

### `e2e/interactions-employee.spec.ts`

Testuje reálné interakce EMPLOYEE portálu.

---

#### TEST 4.1 — Přidání poznámky k termínu
**Precondition:** Terapeut má CONFIRMED termín (ze seedu — dnešní termíny)

**Akce:**
1. Přihlásit terapeuta (`terapeut@pristav.cz`)
2. Navigovat na `/employee/appointments` nebo dashboard
3. Kliknout na CONFIRMED termín
4. Najít sekci "Poznámka" nebo "Přidat poznámku k sezení"
5. Zadat text: "E2E testovací poznámka k sezení"
6. Uložit/odeslat

**Assertions:**
- Toast o úspěšném uložení
- Poznámka je viditelná v detailu termínu
- `apiGet(page, "/appointments/:id")` nebo notes endpoint obsahuje text poznámky

---

#### TEST 4.2 — Zadání domácího úkolu klientovi
**Akce:**
1. Přihlásit terapeuta
2. Navigovat na `/employee/clients` nebo detail klienta
3. Kliknout na "Martin Svoboda"
4. Kliknout "Přidat domácí úkol" / "Nový úkol"
5. Vyplnit:
   - Název: "Cvičení ruky — 10 opakování"
   - Popis: "Stisknutí pěsti, držet 5 vteřin"
6. Odeslat

**Assertions:**
- Toast o úspěšném uložení
- Úkol se zobrazí v klientově seznamu úkolů
- `apiGet(page, "/homework?clientId=5")` obsahuje nový úkol

---

#### TEST 4.3 — Dokončení domácího úkolu (pohled klienta)
**Precondition:** Existuje ACTIVE domácí úkol pro Martina Svobodu (z TEST 4.2)

**Akce:**
1. Přihlásit klienta (`klient@pristav.cz`)
2. Navigovat na `/client/homework`
3. Kliknout "Označit jako splněné" u úkolu "Cvičení ruky"

**Assertions:**
- Status úkolu se změní na "Splněno"/"COMPLETED"
- Tlačítko zmizí nebo se deaktivuje
- `apiGet(page, "/homework")` úkol má `status: "COMPLETED"`

---

### `e2e/form-validation.spec.ts`

Testuje validaci formulářů — co se stane když uživatel odesílá špatná data.

---

#### TEST 5.1 — Login — prázdné pole
**Akce:**
1. Jít na `/login`
2. Kliknout přihlásit bez vyplnění formuláře

**Assertions:**
- Formulář se neodeslal (stránka zůstane na loginu)
- Validační chyba je viditelná

---

#### TEST 5.2 — Login — neexistující email
**Akce:**
1. Zadat "neexistuje@test.cz" / "SpatneHeslo123!"
2. Kliknout přihlásit

**Assertions:**
- Error zpráva `text=/Neplatné přihlašovací údaje|neplatný email|invalid/i`
- Formulář zůstane prázdný nebo s emailem

---

#### TEST 5.3 — Login — lockout po 5 pokusech
**Akce:**
1. 5× zadat správný email se špatným heslem

**Assertions:**
- 6. pokus → zobrazí se `text=/dočasně zablokován|too many attempts|příliš mnoho/i`

---

#### TEST 5.4 — Registrace nového uživatele — slabé heslo
**Precondition:** Admin je přihlášen

**Akce:**
1. `/admin/users` → Přidat uživatele
2. Zadat heslo "abc" (příliš slabé)
3. Odeslat

**Assertions:**
- Error `text=/alespoň 8 znaků|heslo musí/i` NEBO API vrátí 400
- Uživatel nebyl vytvořen

---

### `e2e/password-reset.spec.ts`

---

#### TEST 6.1 — Odeslání reset emailu (existující email)
**Akce:**
1. Jít na `/forgot-password`
2. Zadat `klient@pristav.cz`
3. Kliknout Odeslat

**Assertions:**
- Zobrazí se zpráva `text=/pokud.*účet.*existuje|email.*odeslán|zkontrolujte.*email/i`
- HTTP 200 z API: `apiPost(page, "/auth/forgot-password", { email: "klient@pristav.cz" })`

---

#### TEST 6.2 — Reset s neexistujícím emailem (anti-enumeration)
**Akce:**
1. Zadat `neexistuje@test.cz`
2. Odeslat

**Assertions:**
- Zobrazí se **stejná** zpráva jako pro existující email (anti-enumeration protection)

---

#### TEST 6.3 — Reset s platným tokenem
**Precondition:** Vygenerovat reset token přes `apiPost(page, "/auth/forgot-password", {...})`
a token načíst z DB přes API (nebo použít test endpoint pokud existuje)

**Akce:**
1. Přejít na `/reset-password?token=<platný_token>`
2. Zadat nové heslo "NewPass456!"
3. Potvrdit heslo
4. Odeslat

**Assertions:**
- Redirect na `/login`
- Login s novým heslem funguje

---

#### TEST 6.4 — Reset s prošlým/neplatným tokenem
**Akce:**
1. Přejít na `/reset-password?token=neplatny-token-xyz`
2. Odeslat

**Assertions:**
- Error `text=/platnost|vypršel|neplatný|invalid/i`

---

### `e2e/notifications.spec.ts`

---

#### TEST 7.1 — Přečtení notifikace kliknutím
**Precondition:** Klient má nepřečtené notifikace (seed: 2 notifikace)

**Akce:**
1. Přihlásit klienta
2. Kliknout na NotificationBell ikonu (v hlavičce/sidebaru)
3. Kliknout na první notifikaci

**Assertions:**
- Badge počtu se sníží
- Notifikace je označena jako přečtená (vizuálně — jiné pozadí nebo bez tučného textu)

---

#### TEST 7.2 — Označit vše jako přečtené
**Akce:**
1. Přihlásit klienta
2. Navigovat na `/notifications`
3. Kliknout "Označit vše jako přečtené"

**Assertions:**
- `apiGet(page, "/notifications/unread-count").data.count` = 0
- Všechny notifikace zobrazeny jako přečtené

---

## ČÁST 2 — Nové API testy (behaviorální)

Stávající API testy jsou věcné. Přidat testy pro 27 nepokrytých route souborů.
Každý test ověřuje **skutečný výsledek operace**, ne jen HTTP status.

### Priorita A — Kritické (4 soubory)

#### `totp.test.ts`
16 testovacích případů:
- Setup vrátí secret + QR kód
- verify-setup s platným TOTP tokenem → `enabled=true` v DB, vrátí backup codes
- verify-setup s neplatným tokenem → 401
- disable pro CLIENT → OK; pro ADMIN → 403
- 2FA login flow: login → `requires2FA: true` + `pendingToken` → verify → plný JWT
- Backup kód funguje jednou, podruhé 401
- Regenerace backup kódů vyžaduje platný TOTP

#### `gdpr.test.ts`
15 testovacích případů pokrývajících consent, erasure, access log, stats + RBAC pro každý endpoint

#### `booking-v2.test.ts`
Přečíst celý soubor (899 řádků), identifikovat všechny endpointy, pokrýt:
- Terapeut vytvoří open slot → slot je viditelný pro klienty
- Klient zarezervuje slot → slot se změní na "booked", kredity se odečtou
- Slot nelze rezervovat dvakrát → 409
- RBAC checks

#### `cancellations.test.ts`
9 testovacích případů: vytvoření záznamu, dopad na appointment status,
paginace, filtrování, RBAC

### Priorita B — Vysoká (15 souborů)
`appointment-reschedule`, `homework`, `questionnaires`, `therapy-reports`,
`gamification`, `exercise-library`, `session-templates`, `insurance`,
`insurance-vouchers`, `intensive-therapy`, `intensive-blocks`, `off-peak`,
`analytics`, `video`, `wellbeing`

Každý soubor: min. 5 testovacích případů pokrývající CRUD + RBAC + validaci.

### Priorita C — Střední (8 souborů)
`emergency`, `first-visit-followup`, `export`, `therapist-services`,
`notification-log`, `monitoring`, `heatmap`, `ai-summary`

---

## ČÁST 3 — Nové web unit testy (interakční)

Místo "renderuje správně" testovat **co se stane když uživatel interaguje**.

#### `src/components/__tests__/RouteGuard.test.tsx`
- Neautentizovaný user → `router.push('/login')` byl zavolán
- Špatná role → `router.push('/unauthorized')`
- Správná role → children jsou renderovány
- Loading state → spinner, bez redirectu

#### `src/contexts/__tests__/AuthContext.test.tsx`
- `login()` — volá `/auth/login`, nastaví token v paměti, `user` state se změní
- `login()` se špatným heslem → `error` state nastaven
- `login()` → `requires2FA: true` → stav `pending2FA`, `pendingToken` uložen
- Auto-refresh: po 12 minutách `setInterval` zavolá `/auth/refresh`
- `logout()` → volá `/auth/logout`, vyčistí token

#### `src/lib/__tests__/api.test.ts`
- `api.get<T>()` — správně parsuje JSON response do typed objektu
- 401 response → automaticky zavolá `/auth/refresh`, zopakuje původní request
- Refresh selže → zavolá `onAuthError` callback (logout)
- `api.post()` — správně nastaví `Content-Type: application/json` a serializuje body

#### `src/lib/__tests__/utils.test.ts`
- `formatCurrency(1234.5)` → `"1 234,50 Kč"`
- `formatCurrency(0)` → `"0,00 Kč"`
- `formatCurrency(-500)` → záporná hodnota správně

#### `src/components/__tests__/DataTable.test.tsx`
- Renderuje správné hodnoty z `data` prop
- `isLoading=true` → skeleton viditelný, data ne
- Kliknutí na řádek → `onRowClick` callback zavolán se správným objektem
- Prázdná data → zobrazí empty state slot

---

## ČÁST 4 — Rozšíření existujících E2E specs

Tyto testy se přidají do stávajících souborů (nenahrazují je, doplní).

### Přidat do `e2e/auth.spec.ts`:
- Přihlášení 5× se špatným heslem → lockout zpráva
- Session persistence: po reload stránky zůstane přihlášen (refresh token funguje)

### Přidat do `e2e/admin-flow.spec.ts`:
- Test 2 (Users): Vyhledat "Jana" → viditelná jen Jana Nováková
- Test 3 (Services): Kliknout edit na Neurorehabilitaci → formulář se předvyplní správnými hodnotami

### Přidat do `e2e/client-flow.spec.ts`:
- Test 3 (Appointments): kliknout na termín → detail termínu se zobrazí s názvem služby a jménem terapeuta

---

## ČÁST 5 — Sdílené testovací utility

### `apps/api/src/__tests__/helpers/setup.ts`
Shared factory pro API testy — odstraní opakující se boilerplate:
```ts
export async function buildTestApp(): Promise<FastifyInstance>
export async function seedTestUsers(): Promise<{ admin: User, reception: User, employee: User, client: User }>
export const FULL_MIGRATION_SQL: string
```

### Rozšíření `apps/web/src/mocks/handlers.ts`
Přidat MSW handlery pro unit testy frontendu:
- `GET /api/auth/me` → mock user podle nastavené role
- `POST /api/auth/login` → mock token response
- `GET /api/services` → 3 mock služby
- `GET /api/credits/balance` → `{ balance: 4300 }`
- `GET /api/notifications` → 2 mock notifikace
- `POST /api/auth/refresh` → nový mock token (pro API client test)

---

## Pořadí implementace

| Pořadí | Co | Dopad |
|---|---|---|
| 1 | `interactions-client.spec.ts` | Nejviditelnější — hlavní client flows |
| 2 | `interactions-admin.spec.ts` | CRUD služby + uživatelé |
| 3 | `interactions-reception.spec.ts` | Credit request lifecycle |
| 4 | `form-validation.spec.ts` | Edge cases + validace |
| 5 | `password-reset.spec.ts` + `notifications.spec.ts` | Bezpečnostní flows |
| 6 | `interactions-employee.spec.ts` | Terapeutské funkce |
| 7 | Shared API helper + `totp.test.ts` + `gdpr.test.ts` | API bezpečnostní testy |
| 8 | `cancellations.test.ts` + `booking-v2.test.ts` | Kritické business API |
| 9 | Priorita B API testy (15 souborů) | Pokrytí zbývajících routes |
| 10 | MSW rozšíření + web unit testy | Frontend unit testy |
| 11 | Priorita C API testy + accessibility | Zbývající mezery |

---

## Výsledný stav po implementaci

| Oblast | Před | Po |
|---|---|---|
| E2E testy: smoke | 90 cases (jestli stránka načte) | Nahrazeny/doplněny behavioral testy |
| E2E testy: interakce | **0** | ~80 behavioral cases |
| API testy | 655 cases (71 souborů) | ~900 cases (98 souborů) |
| Nepokryté API routes | 31 | ~4 |
| Web unit testy | 9 cases | ~50 cases |
